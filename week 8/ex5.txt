Physical and virtual addresses

		Physical						Vitual

Computed by MMU					Generated by CPU

We cannot see the physical      We can see the virtual address
address

It is not possible to access    We can use the virtual address to
the address directly            access the physical address
